import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  navMarginasd: {
    height: '96px',
    width: '100%',
  },

  title: {
    color: 'white',
    width: '100%',
    textAlign: 'center',
    marginTop: '10%',
  },
});

export default useStyles;
